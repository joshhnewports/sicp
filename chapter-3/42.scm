Originally, a call to withdraw would start a call to (protected withdraw). Here, a call to withdraw references
a local procedure that is created on make-account. Hence (protected withdraw) always exists. I couldn't tell you
anything more.
