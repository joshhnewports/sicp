;;The interleaving of the stream-map and pairs continues forever because there is no delay.
;;With the cons-stream in the original definition, there is a delay. Hence when pairs is called there is a delay preventing the next pairs call.
